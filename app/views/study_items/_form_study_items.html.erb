 <% study_item.errors.full_messages.each do |message| %>
    <p><%= message%></p>
 <%end%>
 
 
<%= form_with(model:study_item) do |f| %>
    <div>
        
        <dt><%= f.label :title,'Titulo' %>
        <%= f.text_field :title, required:'required' %></dt>
       
    </div>

    <div><br>
        <dt><%= f.label :category_id,'Categoria' %>
        <%#= f.select :category_id, ['Ruby', 'Rails','Fundamentos'].sort, prompt: 'Escolha uma categoria' %>
        <%= f.collection_select :category_id, Category.all.order(:title), :id, :title %></dt>
    </div>
    
    <!--<div>
        <span>Feito?</span>
        <%= f.label :done,'Sim' %>
        <%= f.radio_button :done, 'true' %>
        <%= f.label :done,'Não' %>
        <%= f.radio_button :done, 'false',checked: true %>
    </div> -->

    <div><br>
        <dt><%= f.submit 'Salvar',class: 'button'%></dt>
    </div>
    
<% end %>