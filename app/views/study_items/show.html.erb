<%= @study_item.title %>

<%if @study_item.done? %>
  ==> Feito
  <%= button_to 'Macar como desfeito', mark_as_done_study_item_path(@study_item), method: :patch %>
<%else%>
  <%= button_to 'Macar como feito', mark_as_done_study_item_path(@study_item), method: :patch %>
<%end%>






<%#study_item.each do |si|%>
 
  <!--li><%#=si.title%></li-->
 
<%#end%> 