<ul>

<%@study_items.each do |si| %>
    <li>
    <%#=link_to(si.category,study_item_path(si.category),class: 'button bouncy')%>
    <%= link_to(si.full_title,study_item_path(si.id))%>
    </li>
<% end %>
</ul>


<%= link_to('Cadastrar Novo Item',new_study_item_path) %>