

<%if @study_items.empty?%>
    Sem Itens de estudo
<%else%>
    
    <%@study_items.each do |si| %>
        <dt>     <%#=link_to(si.category,study_item_path(si.category),class: 'button bouncy')%>
        <%= link_to(si.full_title,study_item_path(si.id), class: 'button')%>
       </dt>
    <% end %>
<% end %>
